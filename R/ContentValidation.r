# Looker API 3.1 Reference
#
# ### Authorization  The classic method of API authorization uses Looker **API3** credentials for authorization and access control. Looker admins can create API3 credentials on Looker's **Admin/Users** page.  API 4.0 adds additional ways to authenticate API requests, including OAuth and CORS requests.  For details, see [Looker API Authorization](https://looker.com/docs/r/api/authorization).   ### API Explorer  The API Explorer is a Looker-provided utility with many new and unique features for learning and using the Looker API and SDKs. It is a replacement for the 'api-docs' page currently provided on Looker instances.  For details, see the [API Explorer documentation](https://looker.com/docs/r/api/explorer).   ### Looker Language SDKs  The Looker API is a RESTful system that should be usable by any programming language capable of making HTTPS requests. SDKs for a variety of programming languages are also provided to streamline using the API. Looker has an OpenSource [sdk-codegen project](https://github.com/looker-open-source/sdk-codegen) that provides several language SDKs. Language SDKs generated by `sdk-codegen` have an Authentication manager that can automatically authenticate API requests when needed.  For details on available Looker SDKs, see [Looker API Client SDKs](https://looker.com/docs/r/api/client_sdks).   ### API Versioning  Future releases of Looker expand the latest API version release-by-release to securely expose more and more of the core power of the Looker platform to API client applications. API endpoints marked as \"beta\" may receive breaking changes without warning (but we will try to avoid doing that). Stable (non-beta) API endpoints should not receive breaking changes in future releases.  For details, see [Looker API Versioning](https://looker.com/docs/r/api/versioning).   ### Try It Out!  This section describes the existing 'api-docs' page available on Looker instances. We recommend using the [API Explorer](https://looker.com/docs/r/api/explorer) instead.  The 'api-docs' page served by the Looker instance includes 'Try It Out!' buttons for each API method. After logging in with API3 credentials, you can use the \"Try It Out!\" buttons to call the API directly from the documentation page to interactively explore API features and responses.  **NOTE**! With great power comes great responsibility: The \"Try It Out!\" button makes API calls to your live Looker instance. Be especially careful with destructive API operations such as `delete_user` or similar. There is no \"undo\" for API operations. (API Explorer's \"Run It\" feature requires a check mark before running API operations that can change data.)   ### In This Release  The following are a few examples of noteworthy items that have changed between API 3.0 and API 3.1. For more comprehensive coverage of API changes, please see the release notes for your Looker release.  ### Examples of new things added in API 3.1 (compared to API 3.0):  * [Dashboard construction](#!/3.1/Dashboard/) APIs * [Themes](#!/3.1/Theme/) and [custom color collections](#!/3.1/ColorCollection) APIs * Create and run [SQL Runner](#!/3.1/Query/run_sql_query) queries * Create and run [merged results](#!/3.1/Query/create_merge_query) queries * Create and modify [dashboard filters](#!/3.1/Dashboard/create_dashboard_filter) * Create and modify [password requirements](#!/3.1/Auth/password_config)  ### Deprecated in API 3.0  The following functions and properties have been deprecated in API 3.0.  They continue to exist and work in API 3.0 for the next several Looker releases but they have not been carried forward to API 3.1:  * Dashboard Prefetch functions * User access_filter functions * User API 1.0 credentials functions * Space.is_root and Space.is_user_root properties. Use Space.is_shared_root and Space.is_users_root instead.  ### Semantic changes in API 3.1:  * [all_looks()](#!/3.1/Look/all_looks) no longer includes soft-deleted looks, matching [all_dashboards()](#!/3.1/Dashboard/all_dashboards) behavior. You can find soft-deleted looks using [search_looks()](#!/3.1/Look/search_looks) with the `deleted` param set to True. * [all_spaces()](#!/3.1/Space/all_spaces) no longer includes duplicate items * [search_users()](#!/3.1/User/search_users) no longer accepts Y,y,1,0,N,n for Boolean params, only \"true\" and \"false\". * For greater client and network compatibility, [render_task_results](#!/3.1/RenderTask/render_task_results) now returns HTTP status **202 Accepted** instead of HTTP status **102 Processing** * [all_running_queries()](#!/3.1/Query/all_running_queries) and [kill_query](#!/3.1/Query/kill_query) functions have moved into the [Query](#!/3.1/Query/) function group.  The API Explorer can be used to [interactively compare](https://looker.com/docs/r/api/explorer#comparing_api_versions) the differences between API 3.1 and 4.0.   ### API and SDK Support Policies  Looker API versions and language SDKs have varying support levels. Please read the API and SDK [support policies](https://looker.com/docs/r/api/support-policy) for more information.   
#
# OpenAPI spec version: 3.1.0
# 
# Generated by: https://github.com/swagger-api/swagger-codegen.git

#' ContentValidation Class
#'
#' @field content_with_errors 
#' @field computation_time 
#' @field total_looks_validated 
#' @field total_dashboard_elements_validated 
#' @field total_dashboard_filters_validated 
#' @field total_scheduled_plans_validated 
#' @field total_alerts_validated 
#' @field total_explores_validated 
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ContentValidation <- R6::R6Class(
  'ContentValidation',
  public = list(
    `content_with_errors` = NULL,
    `computation_time` = NULL,
    `total_looks_validated` = NULL,
    `total_dashboard_elements_validated` = NULL,
    `total_dashboard_filters_validated` = NULL,
    `total_scheduled_plans_validated` = NULL,
    `total_alerts_validated` = NULL,
    `total_explores_validated` = NULL,
    initialize = function(`content_with_errors`, `computation_time`, `total_looks_validated`, `total_dashboard_elements_validated`, `total_dashboard_filters_validated`, `total_scheduled_plans_validated`, `total_alerts_validated`, `total_explores_validated`){
      if (!missing(`content_with_errors`)) {
        stopifnot(is.list(`content_with_errors`), length(`content_with_errors`) != 0)
        lapply(`content_with_errors`, function(x) stopifnot(R6::is.R6(x)))
        self$`content_with_errors` <- `content_with_errors`
      }
      if (!missing(`computation_time`)) {
        stopifnot(is.numeric(`computation_time`), length(`computation_time`) == 1)
        self$`computation_time` <- `computation_time`
      }
      if (!missing(`total_looks_validated`)) {
        stopifnot(is.numeric(`total_looks_validated`), length(`total_looks_validated`) == 1)
        self$`total_looks_validated` <- `total_looks_validated`
      }
      if (!missing(`total_dashboard_elements_validated`)) {
        stopifnot(is.numeric(`total_dashboard_elements_validated`), length(`total_dashboard_elements_validated`) == 1)
        self$`total_dashboard_elements_validated` <- `total_dashboard_elements_validated`
      }
      if (!missing(`total_dashboard_filters_validated`)) {
        stopifnot(is.numeric(`total_dashboard_filters_validated`), length(`total_dashboard_filters_validated`) == 1)
        self$`total_dashboard_filters_validated` <- `total_dashboard_filters_validated`
      }
      if (!missing(`total_scheduled_plans_validated`)) {
        stopifnot(is.numeric(`total_scheduled_plans_validated`), length(`total_scheduled_plans_validated`) == 1)
        self$`total_scheduled_plans_validated` <- `total_scheduled_plans_validated`
      }
      if (!missing(`total_alerts_validated`)) {
        stopifnot(is.numeric(`total_alerts_validated`), length(`total_alerts_validated`) == 1)
        self$`total_alerts_validated` <- `total_alerts_validated`
      }
      if (!missing(`total_explores_validated`)) {
        stopifnot(is.numeric(`total_explores_validated`), length(`total_explores_validated`) == 1)
        self$`total_explores_validated` <- `total_explores_validated`
      }
    },
    toJSON = function() {
      ContentValidationObject <- list()
      if (!is.null(self$`content_with_errors`)) {
        ContentValidationObject[['content_with_errors']] <- lapply(self$`content_with_errors`, function(x) x$toJSON())
      }
      if (!is.null(self$`computation_time`)) {
        ContentValidationObject[['computation_time']] <- self$`computation_time`
      }
      if (!is.null(self$`total_looks_validated`)) {
        ContentValidationObject[['total_looks_validated']] <- self$`total_looks_validated`
      }
      if (!is.null(self$`total_dashboard_elements_validated`)) {
        ContentValidationObject[['total_dashboard_elements_validated']] <- self$`total_dashboard_elements_validated`
      }
      if (!is.null(self$`total_dashboard_filters_validated`)) {
        ContentValidationObject[['total_dashboard_filters_validated']] <- self$`total_dashboard_filters_validated`
      }
      if (!is.null(self$`total_scheduled_plans_validated`)) {
        ContentValidationObject[['total_scheduled_plans_validated']] <- self$`total_scheduled_plans_validated`
      }
      if (!is.null(self$`total_alerts_validated`)) {
        ContentValidationObject[['total_alerts_validated']] <- self$`total_alerts_validated`
      }
      if (!is.null(self$`total_explores_validated`)) {
        ContentValidationObject[['total_explores_validated']] <- self$`total_explores_validated`
      }

      ContentValidationObject
    },
    fromJSON = function(ContentValidationJson) {
      ContentValidationObject <- jsonlite::fromJSON(ContentValidationJson)
      if (!is.null(ContentValidationObject$`content_with_errors`)) {
        self$`content_with_errors` <- lapply(ContentValidationObject$`content_with_errors`, function(x) {
          content_with_errorsObject <- ContentValidatorError$new()
          content_with_errorsObject$fromJSON(jsonlite::toJSON(x, auto_unbox = TRUE))
          content_with_errorsObject
        })
      }
      if (!is.null(ContentValidationObject$`computation_time`)) {
        self$`computation_time` <- ContentValidationObject$`computation_time`
      }
      if (!is.null(ContentValidationObject$`total_looks_validated`)) {
        self$`total_looks_validated` <- ContentValidationObject$`total_looks_validated`
      }
      if (!is.null(ContentValidationObject$`total_dashboard_elements_validated`)) {
        self$`total_dashboard_elements_validated` <- ContentValidationObject$`total_dashboard_elements_validated`
      }
      if (!is.null(ContentValidationObject$`total_dashboard_filters_validated`)) {
        self$`total_dashboard_filters_validated` <- ContentValidationObject$`total_dashboard_filters_validated`
      }
      if (!is.null(ContentValidationObject$`total_scheduled_plans_validated`)) {
        self$`total_scheduled_plans_validated` <- ContentValidationObject$`total_scheduled_plans_validated`
      }
      if (!is.null(ContentValidationObject$`total_alerts_validated`)) {
        self$`total_alerts_validated` <- ContentValidationObject$`total_alerts_validated`
      }
      if (!is.null(ContentValidationObject$`total_explores_validated`)) {
        self$`total_explores_validated` <- ContentValidationObject$`total_explores_validated`
      }
    },
    toJSONString = function() {
       sprintf(
        '{
           "content_with_errors": [%s],
           "computation_time": %d,
           "total_looks_validated": %d,
           "total_dashboard_elements_validated": %d,
           "total_dashboard_filters_validated": %d,
           "total_scheduled_plans_validated": %d,
           "total_alerts_validated": %d,
           "total_explores_validated": %d
        }',
        lapply(self$`content_with_errors`, function(x) paste(x$toJSON(), sep=",")),
        self$`computation_time`,
        self$`total_looks_validated`,
        self$`total_dashboard_elements_validated`,
        self$`total_dashboard_filters_validated`,
        self$`total_scheduled_plans_validated`,
        self$`total_alerts_validated`,
        self$`total_explores_validated`
      )
    },
    fromJSONString = function(ContentValidationJson) {
      ContentValidationObject <- jsonlite::fromJSON(ContentValidationJson)
      self$`content_with_errors` <- lapply(ContentValidationObject$`content_with_errors`, function(x) ContentValidatorError$new()$fromJSON(jsonlite::toJSON(x, auto_unbox = TRUE)))
      self$`computation_time` <- ContentValidationObject$`computation_time`
      self$`total_looks_validated` <- ContentValidationObject$`total_looks_validated`
      self$`total_dashboard_elements_validated` <- ContentValidationObject$`total_dashboard_elements_validated`
      self$`total_dashboard_filters_validated` <- ContentValidationObject$`total_dashboard_filters_validated`
      self$`total_scheduled_plans_validated` <- ContentValidationObject$`total_scheduled_plans_validated`
      self$`total_alerts_validated` <- ContentValidationObject$`total_alerts_validated`
      self$`total_explores_validated` <- ContentValidationObject$`total_explores_validated`
    }
  )
)
