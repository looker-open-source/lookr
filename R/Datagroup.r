# Looker API 3.0 Reference
# 
# ### Authorization  The Looker API uses Looker **API3** credentials for authorization and access control. Looker admins can create API3 credentials on Looker's **Admin/Users** page. Pass API3 credentials to the **/login** endpoint to obtain a temporary access_token. Include that access_token in the Authorization header of Looker API requests. For details, see [Looker API Authorization](https://looker.com/docs/r/api/authorization)  ### Client SDKs  The Looker API is a RESTful system that should be usable by any programming language capable of making HTTPS requests. Client SDKs for a variety of programming languages can be generated from the Looker API's Swagger JSON metadata to streamline use of the Looker API in your applications. A client SDK for Ruby is available as an example. For more information, see [Looker API Client SDKs](https://looker.com/docs/r/api/client_sdks)  ### Try It Out!  The 'api-docs' page served by the Looker instance includes 'Try It Out!' buttons for each API method. After logging in with API3 credentials, you can use the \"Try It Out!\" buttons to call the API directly from the documentation page to interactively explore API features and responses.  ### Versioning  Future releases of Looker will expand this API release-by-release to securely expose more and more of the core power of Looker to API client applications. API endpoints marked as \"beta\" may receive breaking changes without warning. Stable (non-beta) API endpoints should not receive breaking changes in future releases. For more information, see [Looker API Versioning](https://looker.com/docs/r/api/versioning) 
# 
# OpenAPI spec version: 3.0.0
# 
# Generated by: https://github.com/swagger-api/swagger-codegen.git


#' Datagroup Class
#'
#' @field id 
#' @field name 
#' @field model_name 
#' @field trigger_value 
#' @field trigger_error 
#' @field stale_before 
#' @field triggered_at 
#' @field trigger_check_at 
#' @field created_at 
#' @field can 
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
Datagroup <- R6::R6Class(
  'Datagroup',
  public = list(
    `id` = NULL,
    `name` = NULL,
    `model_name` = NULL,
    `trigger_value` = NULL,
    `trigger_error` = NULL,
    `stale_before` = NULL,
    `triggered_at` = NULL,
    `trigger_check_at` = NULL,
    `created_at` = NULL,
    `can` = NULL,
    initialize = function(`id`, `name`, `model_name`, `trigger_value`, `trigger_error`, `stale_before`, `triggered_at`, `trigger_check_at`, `created_at`, `can`){
      if (!missing(`id`)) {
        stopifnot(is.numeric(`id`), length(`id`) == 1)
        self$`id` <- `id`
      }
      if (!missing(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
        self$`name` <- `name`
      }
      if (!missing(`model_name`)) {
        stopifnot(is.character(`model_name`), length(`model_name`) == 1)
        self$`model_name` <- `model_name`
      }
      if (!missing(`trigger_value`)) {
        stopifnot(is.character(`trigger_value`), length(`trigger_value`) == 1)
        self$`trigger_value` <- `trigger_value`
      }
      if (!missing(`trigger_error`)) {
        stopifnot(is.character(`trigger_error`), length(`trigger_error`) == 1)
        self$`trigger_error` <- `trigger_error`
      }
      if (!missing(`stale_before`)) {
        stopifnot(is.numeric(`stale_before`), length(`stale_before`) == 1)
        self$`stale_before` <- `stale_before`
      }
      if (!missing(`triggered_at`)) {
        stopifnot(is.numeric(`triggered_at`), length(`triggered_at`) == 1)
        self$`triggered_at` <- `triggered_at`
      }
      if (!missing(`trigger_check_at`)) {
        stopifnot(is.numeric(`trigger_check_at`), length(`trigger_check_at`) == 1)
        self$`trigger_check_at` <- `trigger_check_at`
      }
      if (!missing(`created_at`)) {
        stopifnot(is.numeric(`created_at`), length(`created_at`) == 1)
        self$`created_at` <- `created_at`
      }
      if (!missing(`can`)) {
        self$`can` <- `can`
      }
    },
    toJSON = function() {
      DatagroupObject <- list()
      if (!is.null(self$`id`)) {
        DatagroupObject[['id']] <- self$`id`
      }
      if (!is.null(self$`name`)) {
        DatagroupObject[['name']] <- self$`name`
      }
      if (!is.null(self$`model_name`)) {
        DatagroupObject[['model_name']] <- self$`model_name`
      }
      if (!is.null(self$`trigger_value`)) {
        DatagroupObject[['trigger_value']] <- self$`trigger_value`
      }
      if (!is.null(self$`trigger_error`)) {
        DatagroupObject[['trigger_error']] <- self$`trigger_error`
      }
      if (!is.null(self$`stale_before`)) {
        DatagroupObject[['stale_before']] <- self$`stale_before`
      }
      if (!is.null(self$`triggered_at`)) {
        DatagroupObject[['triggered_at']] <- self$`triggered_at`
      }
      if (!is.null(self$`trigger_check_at`)) {
        DatagroupObject[['trigger_check_at']] <- self$`trigger_check_at`
      }
      if (!is.null(self$`created_at`)) {
        DatagroupObject[['created_at']] <- self$`created_at`
      }
      if (!is.null(self$`can`)) {
        DatagroupObject[['can']] <- self$`can`
      }

      DatagroupObject
    },
    fromJSON = function(DatagroupJson) {
      DatagroupObject <- jsonlite::fromJSON(DatagroupJson)
      if (!is.null(DatagroupObject$`id`)) {
        self$`id` <- DatagroupObject$`id`
      }
      if (!is.null(DatagroupObject$`name`)) {
        self$`name` <- DatagroupObject$`name`
      }
      if (!is.null(DatagroupObject$`model_name`)) {
        self$`model_name` <- DatagroupObject$`model_name`
      }
      if (!is.null(DatagroupObject$`trigger_value`)) {
        self$`trigger_value` <- DatagroupObject$`trigger_value`
      }
      if (!is.null(DatagroupObject$`trigger_error`)) {
        self$`trigger_error` <- DatagroupObject$`trigger_error`
      }
      if (!is.null(DatagroupObject$`stale_before`)) {
        self$`stale_before` <- DatagroupObject$`stale_before`
      }
      if (!is.null(DatagroupObject$`triggered_at`)) {
        self$`triggered_at` <- DatagroupObject$`triggered_at`
      }
      if (!is.null(DatagroupObject$`trigger_check_at`)) {
        self$`trigger_check_at` <- DatagroupObject$`trigger_check_at`
      }
      if (!is.null(DatagroupObject$`created_at`)) {
        self$`created_at` <- DatagroupObject$`created_at`
      }
      if (!is.null(DatagroupObject$`can`)) {
        self$`can` <- DatagroupObject$`can`
      }
    },
    toJSONString = function() {
       sprintf(
        '{
           "id": %d,
           "name": %s,
           "model_name": %s,
           "trigger_value": %s,
           "trigger_error": %s,
           "stale_before": %d,
           "triggered_at": %d,
           "trigger_check_at": %d,
           "created_at": %d,
           "can": %s
        }',
        self$`id`,
        self$`name`,
        self$`model_name`,
        self$`trigger_value`,
        self$`trigger_error`,
        self$`stale_before`,
        self$`triggered_at`,
        self$`trigger_check_at`,
        self$`created_at`,
        self$`can`
      )
    },
    fromJSONString = function(DatagroupJson) {
      DatagroupObject <- jsonlite::fromJSON(DatagroupJson)
      self$`id` <- DatagroupObject$`id`
      self$`name` <- DatagroupObject$`name`
      self$`model_name` <- DatagroupObject$`model_name`
      self$`trigger_value` <- DatagroupObject$`trigger_value`
      self$`trigger_error` <- DatagroupObject$`trigger_error`
      self$`stale_before` <- DatagroupObject$`stale_before`
      self$`triggered_at` <- DatagroupObject$`triggered_at`
      self$`trigger_check_at` <- DatagroupObject$`trigger_check_at`
      self$`created_at` <- DatagroupObject$`created_at`
      self$`can` <- DatagroupObject$`can`
    }
  )
)
