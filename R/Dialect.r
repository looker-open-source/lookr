# Looker API 3.0 Reference
# 
# ### Authorization  The Looker API uses Looker **API3** credentials for authorization and access control. Looker admins can create API3 credentials on Looker's **Admin/Users** page. Pass API3 credentials to the **/login** endpoint to obtain a temporary access_token. Include that access_token in the Authorization header of Looker API requests. For details, see [Looker API Authorization](https://looker.com/docs/r/api/authorization)  ### Client SDKs  The Looker API is a RESTful system that should be usable by any programming language capable of making HTTPS requests. Client SDKs for a variety of programming languages can be generated from the Looker API's Swagger JSON metadata to streamline use of the Looker API in your applications. A client SDK for Ruby is available as an example. For more information, see [Looker API Client SDKs](https://looker.com/docs/r/api/client_sdks)  ### Try It Out!  The 'api-docs' page served by the Looker instance includes 'Try It Out!' buttons for each API method. After logging in with API3 credentials, you can use the \"Try It Out!\" buttons to call the API directly from the documentation page to interactively explore API features and responses.  ### Versioning  Future releases of Looker will expand this API release-by-release to securely expose more and more of the core power of Looker to API client applications. API endpoints marked as \"beta\" may receive breaking changes without warning. Stable (non-beta) API endpoints should not receive breaking changes in future releases. For more information, see [Looker API Versioning](https://looker.com/docs/r/api/versioning) 
# 
# OpenAPI spec version: 3.0.0
# 
# Generated by: https://github.com/swagger-api/swagger-codegen.git


#' Dialect Class
#'
#' @field name 
#' @field label 
#' @field supports_cost_estimate 
#' @field supports_upload_tables 
#' @field persistent_table_indexes 
#' @field persistent_table_sortkeys 
#' @field persistent_table_distkey 
#' @field supports_streaming 
#' @field automatically_run_sql_runner_snippets 
#' @field connection_tests 
#' @field can 
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
Dialect <- R6::R6Class(
  'Dialect',
  public = list(
    `name` = NULL,
    `label` = NULL,
    `supports_cost_estimate` = NULL,
    `supports_upload_tables` = NULL,
    `persistent_table_indexes` = NULL,
    `persistent_table_sortkeys` = NULL,
    `persistent_table_distkey` = NULL,
    `supports_streaming` = NULL,
    `automatically_run_sql_runner_snippets` = NULL,
    `connection_tests` = NULL,
    `can` = NULL,
    initialize = function(`name`, `label`, `supports_cost_estimate`, `supports_upload_tables`, `persistent_table_indexes`, `persistent_table_sortkeys`, `persistent_table_distkey`, `supports_streaming`, `automatically_run_sql_runner_snippets`, `connection_tests`, `can`){
      if (!missing(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
        self$`name` <- `name`
      }
      if (!missing(`label`)) {
        stopifnot(is.character(`label`), length(`label`) == 1)
        self$`label` <- `label`
      }
      if (!missing(`supports_cost_estimate`)) {
        self$`supports_cost_estimate` <- `supports_cost_estimate`
      }
      if (!missing(`supports_upload_tables`)) {
        self$`supports_upload_tables` <- `supports_upload_tables`
      }
      if (!missing(`persistent_table_indexes`)) {
        stopifnot(is.character(`persistent_table_indexes`), length(`persistent_table_indexes`) == 1)
        self$`persistent_table_indexes` <- `persistent_table_indexes`
      }
      if (!missing(`persistent_table_sortkeys`)) {
        stopifnot(is.character(`persistent_table_sortkeys`), length(`persistent_table_sortkeys`) == 1)
        self$`persistent_table_sortkeys` <- `persistent_table_sortkeys`
      }
      if (!missing(`persistent_table_distkey`)) {
        stopifnot(is.character(`persistent_table_distkey`), length(`persistent_table_distkey`) == 1)
        self$`persistent_table_distkey` <- `persistent_table_distkey`
      }
      if (!missing(`supports_streaming`)) {
        self$`supports_streaming` <- `supports_streaming`
      }
      if (!missing(`automatically_run_sql_runner_snippets`)) {
        self$`automatically_run_sql_runner_snippets` <- `automatically_run_sql_runner_snippets`
      }
      if (!missing(`connection_tests`)) {
        stopifnot(is.list(`connection_tests`), length(`connection_tests`) != 0)
        lapply(`connection_tests`, function(x) stopifnot(is.character(x)))
        self$`connection_tests` <- `connection_tests`
      }
      if (!missing(`can`)) {
        self$`can` <- `can`
      }
    },
    toJSON = function() {
      DialectObject <- list()
      if (!is.null(self$`name`)) {
        DialectObject[['name']] <- self$`name`
      }
      if (!is.null(self$`label`)) {
        DialectObject[['label']] <- self$`label`
      }
      if (!is.null(self$`supports_cost_estimate`)) {
        DialectObject[['supports_cost_estimate']] <- self$`supports_cost_estimate`
      }
      if (!is.null(self$`supports_upload_tables`)) {
        DialectObject[['supports_upload_tables']] <- self$`supports_upload_tables`
      }
      if (!is.null(self$`persistent_table_indexes`)) {
        DialectObject[['persistent_table_indexes']] <- self$`persistent_table_indexes`
      }
      if (!is.null(self$`persistent_table_sortkeys`)) {
        DialectObject[['persistent_table_sortkeys']] <- self$`persistent_table_sortkeys`
      }
      if (!is.null(self$`persistent_table_distkey`)) {
        DialectObject[['persistent_table_distkey']] <- self$`persistent_table_distkey`
      }
      if (!is.null(self$`supports_streaming`)) {
        DialectObject[['supports_streaming']] <- self$`supports_streaming`
      }
      if (!is.null(self$`automatically_run_sql_runner_snippets`)) {
        DialectObject[['automatically_run_sql_runner_snippets']] <- self$`automatically_run_sql_runner_snippets`
      }
      if (!is.null(self$`connection_tests`)) {
        DialectObject[['connection_tests']] <- self$`connection_tests`
      }
      if (!is.null(self$`can`)) {
        DialectObject[['can']] <- self$`can`
      }

      DialectObject
    },
    fromJSON = function(DialectJson) {
      DialectObject <- jsonlite::fromJSON(DialectJson)
      if (!is.null(DialectObject$`name`)) {
        self$`name` <- DialectObject$`name`
      }
      if (!is.null(DialectObject$`label`)) {
        self$`label` <- DialectObject$`label`
      }
      if (!is.null(DialectObject$`supports_cost_estimate`)) {
        self$`supports_cost_estimate` <- DialectObject$`supports_cost_estimate`
      }
      if (!is.null(DialectObject$`supports_upload_tables`)) {
        self$`supports_upload_tables` <- DialectObject$`supports_upload_tables`
      }
      if (!is.null(DialectObject$`persistent_table_indexes`)) {
        self$`persistent_table_indexes` <- DialectObject$`persistent_table_indexes`
      }
      if (!is.null(DialectObject$`persistent_table_sortkeys`)) {
        self$`persistent_table_sortkeys` <- DialectObject$`persistent_table_sortkeys`
      }
      if (!is.null(DialectObject$`persistent_table_distkey`)) {
        self$`persistent_table_distkey` <- DialectObject$`persistent_table_distkey`
      }
      if (!is.null(DialectObject$`supports_streaming`)) {
        self$`supports_streaming` <- DialectObject$`supports_streaming`
      }
      if (!is.null(DialectObject$`automatically_run_sql_runner_snippets`)) {
        self$`automatically_run_sql_runner_snippets` <- DialectObject$`automatically_run_sql_runner_snippets`
      }
      if (!is.null(DialectObject$`connection_tests`)) {
        self$`connection_tests` <- DialectObject$`connection_tests`
      }
      if (!is.null(DialectObject$`can`)) {
        self$`can` <- DialectObject$`can`
      }
    },
    toJSONString = function() {
       sprintf(
        '{
           "name": %s,
           "label": %s,
           "supports_cost_estimate": %s,
           "supports_upload_tables": %s,
           "persistent_table_indexes": %s,
           "persistent_table_sortkeys": %s,
           "persistent_table_distkey": %s,
           "supports_streaming": %s,
           "automatically_run_sql_runner_snippets": %s,
           "connection_tests": [%s],
           "can": %s
        }',
        self$`name`,
        self$`label`,
        self$`supports_cost_estimate`,
        self$`supports_upload_tables`,
        self$`persistent_table_indexes`,
        self$`persistent_table_sortkeys`,
        self$`persistent_table_distkey`,
        self$`supports_streaming`,
        self$`automatically_run_sql_runner_snippets`,
        lapply(self$`connection_tests`, function(x) paste(paste0('"', x, '"'), sep=",")),
        self$`can`
      )
    },
    fromJSONString = function(DialectJson) {
      DialectObject <- jsonlite::fromJSON(DialectJson)
      self$`name` <- DialectObject$`name`
      self$`label` <- DialectObject$`label`
      self$`supports_cost_estimate` <- DialectObject$`supports_cost_estimate`
      self$`supports_upload_tables` <- DialectObject$`supports_upload_tables`
      self$`persistent_table_indexes` <- DialectObject$`persistent_table_indexes`
      self$`persistent_table_sortkeys` <- DialectObject$`persistent_table_sortkeys`
      self$`persistent_table_distkey` <- DialectObject$`persistent_table_distkey`
      self$`supports_streaming` <- DialectObject$`supports_streaming`
      self$`automatically_run_sql_runner_snippets` <- DialectObject$`automatically_run_sql_runner_snippets`
      self$`connection_tests` <- DialectObject$`connection_tests`
      self$`can` <- DialectObject$`can`
    }
  )
)
