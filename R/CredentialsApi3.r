# Looker API 3.0 Reference
# 
# ### Authorization  The Looker API uses Looker **API3** credentials for authorization and access control. Looker admins can create API3 credentials on Looker's **Admin/Users** page. Pass API3 credentials to the **/login** endpoint to obtain a temporary access_token. Include that access_token in the Authorization header of Looker API requests. For details, see [Looker API Authorization](https://looker.com/docs/r/api/authorization)  ### Client SDKs  The Looker API is a RESTful system that should be usable by any programming language capable of making HTTPS requests. Client SDKs for a variety of programming languages can be generated from the Looker API's Swagger JSON metadata to streamline use of the Looker API in your applications. A client SDK for Ruby is available as an example. For more information, see [Looker API Client SDKs](https://looker.com/docs/r/api/client_sdks)  ### Try It Out!  The 'api-docs' page served by the Looker instance includes 'Try It Out!' buttons for each API method. After logging in with API3 credentials, you can use the \"Try It Out!\" buttons to call the API directly from the documentation page to interactively explore API features and responses.  ### Versioning  Future releases of Looker will expand this API release-by-release to securely expose more and more of the core power of Looker to API client applications. API endpoints marked as \"beta\" may receive breaking changes without warning. Stable (non-beta) API endpoints should not receive breaking changes in future releases. For more information, see [Looker API Versioning](https://looker.com/docs/r/api/versioning) 
# 
# OpenAPI spec version: 3.0.0
# 
# Generated by: https://github.com/swagger-api/swagger-codegen.git


#' CredentialsApi3 Class
#'
#' @field id 
#' @field client_id 
#' @field created_at 
#' @field is_disabled 
#' @field type 
#' @field url 
#' @field can 
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
CredentialsApi3 <- R6::R6Class(
  'CredentialsApi3',
  public = list(
    `id` = NULL,
    `client_id` = NULL,
    `created_at` = NULL,
    `is_disabled` = NULL,
    `type` = NULL,
    `url` = NULL,
    `can` = NULL,
    initialize = function(`id`, `client_id`, `created_at`, `is_disabled`, `type`, `url`, `can`){
      if (!missing(`id`)) {
        stopifnot(is.numeric(`id`), length(`id`) == 1)
        self$`id` <- `id`
      }
      if (!missing(`client_id`)) {
        stopifnot(is.character(`client_id`), length(`client_id`) == 1)
        self$`client_id` <- `client_id`
      }
      if (!missing(`created_at`)) {
        stopifnot(is.character(`created_at`), length(`created_at`) == 1)
        self$`created_at` <- `created_at`
      }
      if (!missing(`is_disabled`)) {
        self$`is_disabled` <- `is_disabled`
      }
      if (!missing(`type`)) {
        stopifnot(is.character(`type`), length(`type`) == 1)
        self$`type` <- `type`
      }
      if (!missing(`url`)) {
        stopifnot(is.character(`url`), length(`url`) == 1)
        self$`url` <- `url`
      }
      if (!missing(`can`)) {
        self$`can` <- `can`
      }
    },
    toJSON = function() {
      CredentialsApi3Object <- list()
      if (!is.null(self$`id`)) {
        CredentialsApi3Object[['id']] <- self$`id`
      }
      if (!is.null(self$`client_id`)) {
        CredentialsApi3Object[['client_id']] <- self$`client_id`
      }
      if (!is.null(self$`created_at`)) {
        CredentialsApi3Object[['created_at']] <- self$`created_at`
      }
      if (!is.null(self$`is_disabled`)) {
        CredentialsApi3Object[['is_disabled']] <- self$`is_disabled`
      }
      if (!is.null(self$`type`)) {
        CredentialsApi3Object[['type']] <- self$`type`
      }
      if (!is.null(self$`url`)) {
        CredentialsApi3Object[['url']] <- self$`url`
      }
      if (!is.null(self$`can`)) {
        CredentialsApi3Object[['can']] <- self$`can`
      }

      CredentialsApi3Object
    },
    fromJSON = function(CredentialsApi3Json) {
      CredentialsApi3Object <- jsonlite::fromJSON(CredentialsApi3Json)
      if (!is.null(CredentialsApi3Object$`id`)) {
        self$`id` <- CredentialsApi3Object$`id`
      }
      if (!is.null(CredentialsApi3Object$`client_id`)) {
        self$`client_id` <- CredentialsApi3Object$`client_id`
      }
      if (!is.null(CredentialsApi3Object$`created_at`)) {
        self$`created_at` <- CredentialsApi3Object$`created_at`
      }
      if (!is.null(CredentialsApi3Object$`is_disabled`)) {
        self$`is_disabled` <- CredentialsApi3Object$`is_disabled`
      }
      if (!is.null(CredentialsApi3Object$`type`)) {
        self$`type` <- CredentialsApi3Object$`type`
      }
      if (!is.null(CredentialsApi3Object$`url`)) {
        self$`url` <- CredentialsApi3Object$`url`
      }
      if (!is.null(CredentialsApi3Object$`can`)) {
        self$`can` <- CredentialsApi3Object$`can`
      }
    },
    toJSONString = function() {
       sprintf(
        '{
           "id": %d,
           "client_id": %s,
           "created_at": %s,
           "is_disabled": %s,
           "type": %s,
           "url": %s,
           "can": %s
        }',
        self$`id`,
        self$`client_id`,
        self$`created_at`,
        self$`is_disabled`,
        self$`type`,
        self$`url`,
        self$`can`
      )
    },
    fromJSONString = function(CredentialsApi3Json) {
      CredentialsApi3Object <- jsonlite::fromJSON(CredentialsApi3Json)
      self$`id` <- CredentialsApi3Object$`id`
      self$`client_id` <- CredentialsApi3Object$`client_id`
      self$`created_at` <- CredentialsApi3Object$`created_at`
      self$`is_disabled` <- CredentialsApi3Object$`is_disabled`
      self$`type` <- CredentialsApi3Object$`type`
      self$`url` <- CredentialsApi3Object$`url`
      self$`can` <- CredentialsApi3Object$`can`
    }
  )
)
