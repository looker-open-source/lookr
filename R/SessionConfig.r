# Looker API 4.0 (Beta) Reference
#
#  Welcome to the future! API 4.0 co-exists with APIs 3.1 and 3.0. (3.0 should no longer be used.) The \"beta\" tag means updates for API 4.0 may include breaking changes, but as always we will work to minimize them.  ### Authorization  The classic method of API authorization uses Looker **API3** credentials for authorization and access control. Looker admins can create API3 credentials on Looker's **Admin/Users** page.  API 4.0 adds additional ways to authenticate API requests, including OAuth and CORS requests.  For details, see [Looker API Authorization](https://looker.com/docs/r/api/authorization).   ### API Explorer  The API Explorer is a Looker-provided utility with many new and unique features for learning and using the Looker API and SDKs. It is a replacement for the 'api-docs' page currently provided on Looker instances.  For details, see the [API Explorer documentation](https://looker.com/docs/r/api/explorer).   ### Looker Language SDKs  The Looker API is a RESTful system that should be usable by any programming language capable of making HTTPS requests. SDKs for a variety of programming languages are also provided to streamline using the API. Looker has an OpenSource [sdk-codegen project](https://github.com/looker-open-source/sdk-codegen) that provides several language SDKs. Language SDKs generated by `sdk-codegen` have an Authentication manager that can automatically authenticate API requests when needed.  For details on available Looker SDKs, see [Looker API Client SDKs](https://looker.com/docs/r/api/client_sdks).   ### API Versioning  Future releases of Looker expand the latest API version release-by-release to securely expose more and more of the core power of the Looker platform to API client applications. API endpoints marked as \"beta\" may receive breaking changes without warning (but we will try to avoid doing that). Stable (non-beta) API endpoints should not receive breaking changes in future releases.  For details, see [Looker API Versioning](https://looker.com/docs/r/api/versioning).   ### In This Release  API 4.0 version was introduced so we can make adjustments to API functions, parameters, and response types to fix bugs and inconsistencies. These changes fall outside the bounds of non-breaking additive changes we can make to our stable API 3.1.  One benefit of these type adjustments in API 4.0 is dramatically better support for strongly typed languages like TypeScript, Kotlin, Swift, Go, C#, and more.  While API 3.1 is still the de-facto Looker API (\"current\", \"stable\", \"default\", etc), the bulk of our development activity has shifted to API 4.0, where all new features are added.  The API Explorer can be used to [interactively compare](https://looker.com/docs/r/api/explorer#comparing_api_versions) the differences between API 3.1 and 4.0.   ### API and SDK Support Policies  Looker API versions and language SDKs have varying support levels. Please read the API and SDK [support policies](https://looker.com/docs/r/api/support-policy) for more information.   
#
# OpenAPI spec version: 4.0.21.18
# 
# Generated by: https://github.com/swagger-api/swagger-codegen.git


#' SessionConfig Class
#'
#' @field can 
#' @field allow_persistent_sessions 
#' @field session_minutes 
#' @field unlimited_sessions_per_user 
#' @field use_inactivity_based_logout 
#' @field track_session_location 
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite parse_json toJSON
#' @export
SessionConfig <- R6::R6Class(
  'SessionConfig',
  public = list(
    `can` = NULL,
    `allow_persistent_sessions` = NULL,
    `session_minutes` = NULL,
    `unlimited_sessions_per_user` = NULL,
    `use_inactivity_based_logout` = NULL,
    `track_session_location` = NULL,
    initialize = function(`can`, `allow_persistent_sessions`, `session_minutes`, `unlimited_sessions_per_user`, `use_inactivity_based_logout`, `track_session_location`){
      if (!missing(`can`)) {
        self$`can` <- `can`
      }
      if (!missing(`allow_persistent_sessions`)) {
        self$`allow_persistent_sessions` <- `allow_persistent_sessions`
      }
      if (!missing(`session_minutes`)) {
        stopifnot(is.numeric(`session_minutes`), length(`session_minutes`) == 1)
        self$`session_minutes` <- `session_minutes`
      }
      if (!missing(`unlimited_sessions_per_user`)) {
        self$`unlimited_sessions_per_user` <- `unlimited_sessions_per_user`
      }
      if (!missing(`use_inactivity_based_logout`)) {
        self$`use_inactivity_based_logout` <- `use_inactivity_based_logout`
      }
      if (!missing(`track_session_location`)) {
        self$`track_session_location` <- `track_session_location`
      }
    },
    toJSON = function() {
      SessionConfigObject <- list()
      if (!is.null(self$`can`)) {
        SessionConfigObject[['can']] <- self$`can`
      }
      if (!is.null(self$`allow_persistent_sessions`)) {
        SessionConfigObject[['allow_persistent_sessions']] <- self$`allow_persistent_sessions`
      }
      if (!is.null(self$`session_minutes`)) {
        SessionConfigObject[['session_minutes']] <- self$`session_minutes`
      }
      if (!is.null(self$`unlimited_sessions_per_user`)) {
        SessionConfigObject[['unlimited_sessions_per_user']] <- self$`unlimited_sessions_per_user`
      }
      if (!is.null(self$`use_inactivity_based_logout`)) {
        SessionConfigObject[['use_inactivity_based_logout']] <- self$`use_inactivity_based_logout`
      }
      if (!is.null(self$`track_session_location`)) {
        SessionConfigObject[['track_session_location']] <- self$`track_session_location`
      }

      SessionConfigObject
    },
    fromJSONObject = function(SessionConfigJsonObject) {
      SessionConfigObject <- SessionConfigJsonObject #jsonlite::fromJSON(SessionConfigJson, simplifyVector = FALSE)
      if (!is.null(SessionConfigObject$`can`)) {
        self$`can` <- SessionConfigObject$`can`
      }
      if (!is.null(SessionConfigObject$`allow_persistent_sessions`)) {
        self$`allow_persistent_sessions` <- SessionConfigObject$`allow_persistent_sessions`
      }
      if (!is.null(SessionConfigObject$`session_minutes`)) {
        self$`session_minutes` <- SessionConfigObject$`session_minutes`
      }
      if (!is.null(SessionConfigObject$`unlimited_sessions_per_user`)) {
        self$`unlimited_sessions_per_user` <- SessionConfigObject$`unlimited_sessions_per_user`
      }
      if (!is.null(SessionConfigObject$`use_inactivity_based_logout`)) {
        self$`use_inactivity_based_logout` <- SessionConfigObject$`use_inactivity_based_logout`
      }
      if (!is.null(SessionConfigObject$`track_session_location`)) {
        self$`track_session_location` <- SessionConfigObject$`track_session_location`
      }
    },
    fromJSON = function(SessionConfigJson) {
      SessionConfigObject <- jsonlite::fromJSON(SessionConfigJson, simplifyVector = FALSE)
      self$fromJSONObject(SessionConfigObject)
    },
    toJSONString = function() {
       sprintf(
        '{
           "can": %s,
           "allow_persistent_sessions": %s,
           "session_minutes": %s,
           "unlimited_sessions_per_user": %s,
           "use_inactivity_based_logout": %s,
           "track_session_location": %s
        }',
	if(is.null(self$`can`)) 'NULL' else self$`can`,
	if(is.null(self$`allow_persistent_sessions`)) 'NULL' else self$`allow_persistent_sessions`,
	if(is.null(self$`session_minutes`)) 'NULL' else as.character(self$`session_minutes`),
	if(is.null(self$`unlimited_sessions_per_user`)) 'NULL' else self$`unlimited_sessions_per_user`,
	if(is.null(self$`use_inactivity_based_logout`)) 'NULL' else self$`use_inactivity_based_logout`,
	if(is.null(self$`track_session_location`)) 'NULL' else self$`track_session_location`
      )
    },
    fromJSONString = function(SessionConfigJson) {
      SessionConfigObject <- jsonlite::fromJSON(SessionConfigJson, simplifyVector = FALSE)
      self$`can` <- SessionConfigObject$`can`
      self$`allow_persistent_sessions` <- SessionConfigObject$`allow_persistent_sessions`
      self$`session_minutes` <- SessionConfigObject$`session_minutes`
      self$`unlimited_sessions_per_user` <- SessionConfigObject$`unlimited_sessions_per_user`
      self$`use_inactivity_based_logout` <- SessionConfigObject$`use_inactivity_based_logout`
      self$`track_session_location` <- SessionConfigObject$`track_session_location`
    }
  )
)
