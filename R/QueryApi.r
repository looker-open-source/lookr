# Looker API 3.0 Reference
# 
# ### Authorization  The Looker API uses Looker **API3** credentials for authorization and access control. Looker admins can create API3 credentials on Looker's **Admin/Users** page. Pass API3 credentials to the **/login** endpoint to obtain a temporary access_token. Include that access_token in the Authorization header of Looker API requests. For details, see [Looker API Authorization](https://looker.com/docs/r/api/authorization)  ### Client SDKs  The Looker API is a RESTful system that should be usable by any programming language capable of making HTTPS requests. Client SDKs for a variety of programming languages can be generated from the Looker API's Swagger JSON metadata to streamline use of the Looker API in your applications. A client SDK for Ruby is available as an example. For more information, see [Looker API Client SDKs](https://looker.com/docs/r/api/client_sdks)  ### Try It Out!  The 'api-docs' page served by the Looker instance includes 'Try It Out!' buttons for each API method. After logging in with API3 credentials, you can use the \"Try It Out!\" buttons to call the API directly from the documentation page to interactively explore API features and responses.  ### Versioning  Future releases of Looker will expand this API release-by-release to securely expose more and more of the core power of Looker to API client applications. API endpoints marked as \"beta\" may receive breaking changes without warning. Stable (non-beta) API endpoints should not receive breaking changes in future releases. For more information, see [Looker API Versioning](https://looker.com/docs/r/api/versioning) 
# 
# OpenAPI spec version: 3.0.0
# 
# Generated by: https://github.com/swagger-api/swagger-codegen.git

#' @title Query operations
#' @description looker.Query
#'
#' @field path Stores url path of the request.
#' @field apiClient Handles the client-server communication.
#' @field userAgent Set the user agent of the request.
#'
#' @importFrom R6 R6Class
#'
#' @section Methods:
#' \describe{
#'
#' create_query Create Query 
#'
#'
#' create_query_task Run Query Async 
#'
#'
#' query Get Query 
#'
#'
#' query_for_slug Get Query for Slug 
#'
#'
#' query_task Get Async Query Info 
#'
#'
#' query_task_multi_results Get Multiple Async Query Results 
#'
#'
#' query_task_results Get Async Query Results 
#'
#'
#' run_inline_query Run Inline Query 
#'
#'
#' run_query Run Query 
#'
#'
#' run_url_encoded_query Run Url Encoded Query 
#'
#' }
#' 
#' @export
QueryApi <- R6::R6Class(
  'QueryApi',
  public = list(
    userAgent = "Swagger-Codegen/1.0.0/r",
    apiClient = NULL,
    initialize = function(apiClient){
      if (!missing(apiClient)) {
        self$apiClient <- apiClient
      }
      else {
        self$apiClient <- ApiClient$new()
      }
    },
    create_query = function(body, fields, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`fields`)) {
        queryParams['fields'] <- fields
      }

      if (!missing(`body`)) {
        body <- `body`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/queries"
      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body, 
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- Query$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    create_query_task = function(body, limit, apply_formatting, apply_vis, cache, image_width, image_height, generate_drill_links, force_production, cache_only, path_prefix, rebuild_pdts, server_table_calcs, fields, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`limit`)) {
        queryParams['limit'] <- limit
      }

      if (!missing(`apply_formatting`)) {
        queryParams['apply_formatting'] <- apply_formatting
      }

      if (!missing(`apply_vis`)) {
        queryParams['apply_vis'] <- apply_vis
      }

      if (!missing(`cache`)) {
        queryParams['cache'] <- cache
      }

      if (!missing(`image_width`)) {
        queryParams['image_width'] <- image_width
      }

      if (!missing(`image_height`)) {
        queryParams['image_height'] <- image_height
      }

      if (!missing(`generate_drill_links`)) {
        queryParams['generate_drill_links'] <- generate_drill_links
      }

      if (!missing(`force_production`)) {
        queryParams['force_production'] <- force_production
      }

      if (!missing(`cache_only`)) {
        queryParams['cache_only'] <- cache_only
      }

      if (!missing(`path_prefix`)) {
        queryParams['path_prefix'] <- path_prefix
      }

      if (!missing(`rebuild_pdts`)) {
        queryParams['rebuild_pdts'] <- rebuild_pdts
      }

      if (!missing(`server_table_calcs`)) {
        queryParams['server_table_calcs'] <- server_table_calcs
      }

      if (!missing(`fields`)) {
        queryParams['fields'] <- fields
      }

      if (!missing(`body`)) {
        body <- `body`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/query_tasks"
      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body, 
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- QueryTask$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    query = function(query_id, fields, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`fields`)) {
        queryParams['fields'] <- fields
      }

      urlPath <- "/queries/{query_id}"
      if (!missing(`query_id`)) {
        urlPath <- gsub(paste0("\\{", "query_id", "\\}"), `query_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body, 
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- Query$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    query_for_slug = function(slug, fields, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`fields`)) {
        queryParams['fields'] <- fields
      }

      urlPath <- "/queries/slug/{slug}"
      if (!missing(`slug`)) {
        urlPath <- gsub(paste0("\\{", "slug", "\\}"), `slug`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body, 
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- Query$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    query_task = function(query_task_id, fields, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`fields`)) {
        queryParams['fields'] <- fields
      }

      urlPath <- "/query_tasks/{query_task_id}"
      if (!missing(`query_task_id`)) {
        urlPath <- gsub(paste0("\\{", "query_task_id", "\\}"), `query_task_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body, 
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- QueryTask$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    query_task_multi_results = function(query_task_ids, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`query_task_ids`)) {
        queryParams['query_task_ids'] <- query_task_ids
      }

      urlPath <- "/query_tasks/multi_results"
      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body, 
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- Character$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    query_task_results = function(query_task_id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/query_tasks/{query_task_id}/results"
      if (!missing(`query_task_id`)) {
        urlPath <- gsub(paste0("\\{", "query_task_id", "\\}"), `query_task_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body, 
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- Character$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    run_inline_query = function(result_format, body, limit, apply_formatting, apply_vis, cache, image_width, image_height, generate_drill_links, force_production, cache_only, path_prefix, rebuild_pdts, server_table_calcs, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`limit`)) {
        queryParams['limit'] <- limit
      }

      if (!missing(`apply_formatting`)) {
        queryParams['apply_formatting'] <- apply_formatting
      }

      if (!missing(`apply_vis`)) {
        queryParams['apply_vis'] <- apply_vis
      }

      if (!missing(`cache`)) {
        queryParams['cache'] <- cache
      }

      if (!missing(`image_width`)) {
        queryParams['image_width'] <- image_width
      }

      if (!missing(`image_height`)) {
        queryParams['image_height'] <- image_height
      }

      if (!missing(`generate_drill_links`)) {
        queryParams['generate_drill_links'] <- generate_drill_links
      }

      if (!missing(`force_production`)) {
        queryParams['force_production'] <- force_production
      }

      if (!missing(`cache_only`)) {
        queryParams['cache_only'] <- cache_only
      }

      if (!missing(`path_prefix`)) {
        queryParams['path_prefix'] <- path_prefix
      }

      if (!missing(`rebuild_pdts`)) {
        queryParams['rebuild_pdts'] <- rebuild_pdts
      }

      if (!missing(`server_table_calcs`)) {
        queryParams['server_table_calcs'] <- server_table_calcs
      }

      if (!missing(`body`)) {
        body <- `body`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/queries/run/{result_format}"
      if (!missing(`result_format`)) {
        urlPath <- gsub(paste0("\\{", "result_format", "\\}"), `result_format`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body, 
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- Character$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    run_query = function(query_id, result_format, limit, apply_formatting, apply_vis, cache, image_width, image_height, generate_drill_links, force_production, cache_only, path_prefix, rebuild_pdts, server_table_calcs, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`limit`)) {
        queryParams['limit'] <- limit
      }

      if (!missing(`apply_formatting`)) {
        queryParams['apply_formatting'] <- apply_formatting
      }

      if (!missing(`apply_vis`)) {
        queryParams['apply_vis'] <- apply_vis
      }

      if (!missing(`cache`)) {
        queryParams['cache'] <- cache
      }

      if (!missing(`image_width`)) {
        queryParams['image_width'] <- image_width
      }

      if (!missing(`image_height`)) {
        queryParams['image_height'] <- image_height
      }

      if (!missing(`generate_drill_links`)) {
        queryParams['generate_drill_links'] <- generate_drill_links
      }

      if (!missing(`force_production`)) {
        queryParams['force_production'] <- force_production
      }

      if (!missing(`cache_only`)) {
        queryParams['cache_only'] <- cache_only
      }

      if (!missing(`path_prefix`)) {
        queryParams['path_prefix'] <- path_prefix
      }

      if (!missing(`rebuild_pdts`)) {
        queryParams['rebuild_pdts'] <- rebuild_pdts
      }

      if (!missing(`server_table_calcs`)) {
        queryParams['server_table_calcs'] <- server_table_calcs
      }

      urlPath <- "/queries/{query_id}/run/{result_format}"
      if (!missing(`query_id`)) {
        urlPath <- gsub(paste0("\\{", "query_id", "\\}"), `query_id`, urlPath)
      }

      if (!missing(`result_format`)) {
        urlPath <- gsub(paste0("\\{", "result_format", "\\}"), `result_format`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body, 
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- Character$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    run_url_encoded_query = function(model_name, view_name, result_format, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/queries/models/{model_name}/views/{view_name}/run/{result_format}"
      if (!missing(`model_name`)) {
        urlPath <- gsub(paste0("\\{", "model_name", "\\}"), `model_name`, urlPath)
      }

      if (!missing(`view_name`)) {
        urlPath <- gsub(paste0("\\{", "view_name", "\\}"), `view_name`, urlPath)
      }

      if (!missing(`result_format`)) {
        urlPath <- gsub(paste0("\\{", "result_format", "\\}"), `result_format`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body, 
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- Character$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    }
  )
) 
